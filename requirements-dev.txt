# Test dependencies. Service modules are imported directly, so their runtime
# libraries are needed too — minus the GPU stack, which is imported lazily.
pytest>=8.0
pytest-cov>=5.0

fastapi>=0.115
httpx>=0.27
pydantic>=2.9
sqlalchemy>=2.0
psycopg2-binary>=2.9
celery[redis]>=5.4
chromadb>=0.5
mcp>=1.0,<2  # server targets the v1 API (FastMCP)
